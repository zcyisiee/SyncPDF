# R3 固定字号与排版工程验收

2026-09-23，主控亲自审diff、执行验证。**本批工程门禁通过；完整译文质量与完整后端目标未完成。** 用户新增V3/Apple GPU要求正另接入，本文耗时是CPU基线。

## 已接入

- Knuth–Plass box/glue/penalty段级断行、fitness/badness、软连字符、ICU/CJK断点；cluster边界、实际fallback字体贯穿排版，缺字/空shaping/字节覆盖不全拒绝。正文两端对齐、末行左对齐；标题居中按源几何保持。
- 精确run字号、颜色与源字体角色；修复源行距pt曾被当字号倍数重复相乘。实际glyph ink用于fit，保留源首行matrix基线。**不缩字号或行距**；容纳失败保留原文并明确提示。
- frame限制CropBox、相邻源内容与图形净空；独立审查发现错行双栏会误跨栏，已补真实frame→typeset回归，左锚点按源文字保持。
- 最终保留内容审计抓到p14部分TJ删除使35字符偏移；已修间接Widths、CID后代W/DW与混合W数组、原TJ数值、Tc/Tw和引号操作状态。按绑定的实际code字节边界删除；未知推进、非有限值或不可靠边界拒绝候选发布。Type3仅明确水平FontMatrix度量，未知字体宽不再猜1000或墨迹宽。

## 主控验收

证据根：`tmp/backend-repair/layout-final-review/` 与 `tmp/backend-repair/codex-r1-integration/`。

| 检查 | 结果/证据 |
|---|---|
| workspace test | `r3-workspace-v3.log`：44 suites，641 passed / 0 failed / 6 ignored；之后补Type3宽度专项，`r3-width-final.log` 109 pdf unit + 6真实PDF通过 |
| fmt、strict workspace Clippy、release | 通过；`r3-clippy-v2.log`、`r3-release-v2.log`；最终注释/Type3专项与缺字体宽度分支另做pdf专项 |
| solver | 12项，含135例穷举oracle；unique100段CJK debug约0.36秒，未改变500ms原门限 |
| 真实部分删除 | worker旧实现3例红，经主控修后6例过：间接Widths、正负TJ、Tc/Tw/Tz、中段/尾段/全删及后续Tj、两种quote、缺Widths失败不变性 |
| 真实首页受控短译 | `r3-fixed-frame-p1-v3/`：标题17.2154007pt/基线678.953003；摘要11.9552002pt/基线546.460938；3525其它字符位置不变；已渲染目视标题/摘要居中 |
| p14专项 | `r3-partial-fixed-p14`：全文件91050保留字符核对0变化，qpdf exit0；修前35偏移证据保留 |
| 23页release fake | `r3-retention-fixed-all`：34.91秒wall，50 typeset、49 overflow、63 atom_source_unplaced、40 protected_source_overlap、1 rotated_source_text，0 coverage_gap；主请求1/补救0/缓存0 |
| 全篇内容与快照 | 83910保留字符核对，Unicode/原点/字号/颜色0变化；23快照未ready页CJK污染0；qpdf exit0；RunFinished false / CLI1正确表示仍有未译块 |

旧无文本fixture的早退仍不算真实写回覆盖；6个ignored中manual任务只对本轮相关首页控制样本显式执行，不重复p19调查。源码字体缺Widths新增拒绝边界后，共享页/事务测试的合成字体改为显式Courier 600宽度，原断言保持；另有真实无Widths输入拒绝且对象不变回归，不以删除失败冒充原测试通过。

## 保留限制

- fake不是LLM真实译文，长块overflow与原子回退仍然可见；不声称论文全篇翻译成功。
- 无Widths简单字体（包括部分Standard14输入）目前拒绝删除；通用PDFium宽度API参数实际为Unicode，不能当源code/GID使用。宽度只读审查证据在`../codex-r3-font-width-audit/tmp/backend-repair/font-width-audit/report.md`。
- RTL视觉shaping已有范围保护，但ActualText逻辑顺序未完成；不宣称完整RTL支持。
- 行内公式/图形原子真绘制、链接点击框、A3 dual、中文目录、字体/字号编辑重编译及真实LLM全篇验收仍待后续。当前GPU工作只读报告在`../codex-v3-gpu-probe/tmp/backend-repair/v3-gpu-report.md`，Python成功不算Rust成功。

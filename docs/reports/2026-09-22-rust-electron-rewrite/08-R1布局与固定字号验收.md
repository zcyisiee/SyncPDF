# R1 布局与固定字号基础验收

2026-09-22，主控亲自 review、编译和运行。结论：本批绑定/布局覆盖/字体稳定性/固定字号失败保护的工程验收通过，进入依赖这些前置的 R2；完整后端和真实译文质量未通过，仍持续 active。

## 本批改动

- `20c1242b` 字体资源稳定编号，基线/容纳使用实际字体度量。
- `d23dac3a` 默认固定字号/行距；`1adeea67` 放不下时保留原文并明确提示；`c3850be6` 验证可容纳与不可容纳两条实际 PDF 路径。
- `fae8e99f` p7 正文续行依据完整物理行拆出，未借 Caption 扩框。
- p15 依据真实三条源横线、Algorithm 标题和贯通栏缝拆开算法与说明；缺证据/相交/多框拒绝。可译段与保留区域共享字形时暂时保留整段并明确提示，完整行内原子实现待后续。

## 主树验证

日志 `tmp/backend-repair/layout-final-review/`：真实 23 页 probe 显式通过（p7/p15 缺口为 0，其余 21 页分区不变），workspace **567 passed、0 failed、4 ignored**，strict Clippy、fmt、release 通过。两个 manual probe 已分别显式执行；另两个 ignored 为 doctest。旧 ci-test 无文本的早退仍为 skip，不算真实文本覆盖。

`tmp/backend-repair/font-stability/real-cross-run.json`：两次独立选 p1/p3 与全篇的相同页在 144dpi 原始像素完全相同；未选 21 页保持，qpdf 三份 exit0。该证据只证明确定性。

最终 release 运行 `tmp/backend-repair/codex-r1-integration/r1-final-safe-all/`：23 页，34.86 秒，23 个 PageReady，coverage gap 为 0；169 个 typeset_overflow、40 个 protected_source_overlap、1 个 translate_fallback。**没有成功译文**；23 页文字及 144dpi 像素均与源 PDF 完全一致（`source-retention.json`），qpdf exit0（`layout-final-review/final-qpdf.log`）。工程保护有效，不能称翻译交付。以往未就绪页泄漏问题在本次全回退样本不能复现，不能因此标为修复。

## 留给 R2 及后续

- 本次错误自检要求全部 ready 页有 CJK，发 23 个 self_check error 后仍 RunFinished ok:true / CLI exit0；必须修实际译文预期与失败状态传播。
- 一次快照不能写未就绪页；删除/渲染/原子保存后才提交文档与 revision。错误不能吞掉。
- 修复逻辑空格/阅读序及唯一字形归属，保护作者地址脚注，接入 Markdown one-shot。
- 原子实际源绘制、逐 run 样式字号、Knuth–Plass、链接/目录/dual/编辑导出仍未完成。
- 真实 LLM 全篇与旧 bdt 质量对照尚未执行。

唯一当前状态仍为 [task-state](task-state.md)，不能据此关闭完整任务。
